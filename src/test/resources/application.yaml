logging:
  level:
    org:
      springframework:
        security: DEBUG
        
springdoc:
  api-docs:
    enabled: true
    path: /api-docs
    
spring:
  application:
    name: test-TaskManager
  datasource:
    url: jdbc:h2:file:./testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

app:
  jwt: necessary-to-change-the-key-for-jwt-test
  exp: 8640000